# Dependencias
node_modules/

# Segredos e ambiente (credencial de TURN entra aqui quando existir)
.env
.env.*
!.env.example

# Sistema operacional
.DS_Store
Thumbs.db

# Sobras de ferramenta visual (V0, Bolt, etc.) — o redesenho entra como
# referencia, nunca como export colado no repositorio. Ver CLAUDE.md.
components/
lib/
pnpm-lock.yaml
pnpm-workspace.yaml
.next/
.v0-trash/
.vercel/
public/placeholder-*
